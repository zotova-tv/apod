<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cosmos">
    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/splash_image_view"
        android:layout_width="@dimen/splash_image_view_width"
        android:layout_height="@dimen/splash_image_view_height"
        android:layout_gravity="center"
        app:srcCompat="@drawable/ic_splash_screen" />
</FrameLayout>